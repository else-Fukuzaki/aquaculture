{
  "name": "aquaculture",
  "version": "1.0.0",
  "description": "養殖漁業管理システム",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for vanilla JS'",
    "cap:add:ios": "npx cap add ios",
    "cap:copy": "npx cap copy",
    "cap:open:ios": "npx cap open ios",
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
